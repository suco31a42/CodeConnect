<%= link_to transition_path(notification) do %>
  <div class="">
    <%= link_to notification.subject.end_user.name, end_user_path(notification.subject.end_user) %>
    があなたの
    <%= link_to "投稿", post_path(notification.subject.post) %>
    をコメントしました
    <%= "(#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>