<%= link_to transition_path(notification) do %>
  <div class="border p-2">
    <%= link_to notification.subject.end_user.name, end_user_path(notification.subject.end_user), class:"link-primary" %>
    があなたの
    <%= link_to "投稿", post_path(notification.subject.post), class:"link-primary" %>
    をコメントしました
    <%= "(#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>