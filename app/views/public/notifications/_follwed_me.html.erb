<%= link_to transition_path(notification) do %>
  <div class="border p-2">
    <%= link_to notification.subject.follower.name, end_user_path(notification.subject.follower), class:"link-primary" %>
    があなたをフォローしました
    <%= "(#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>