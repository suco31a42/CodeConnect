name: Rails CI/CD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Deploy
      env:
        CODECONNECT_PRIVATE_KYE: ${{ secrets.PRIVATE_KEY }}
        CODECONNECT_USER_NAME: ${{ secrets.USER_NAME }}
        CODECONNECT_HOST_NAME: ${{ secrets.HOST_NAME }}
      run: |
        echo "$CODECONNECT_PRIVATE_KEY" > private_key && chmod 600 private_key
        ssh -o StrictHostKeyChecking=no -i private_key ${CODECONNECT_USER_NAME}@${CODECONNECT_HOST_NAME} 'cd CodeConnect &&
        git pull origin main &&
        ~/.rbenv/shims/bundle install &&
        ~/.rbenv/shims/bundle exec rails assets:precompile RAILS_ENV=production &&
        ~/.rbenv/shims/bundle exec rails db:migrate RAILS_ENV=production &&
        if [[ -e tmp/pids/puma.pid ]];then sudo kill $(cat tmp/pids/puma.pid); echo kill puma process;fi &&
        ~/.rbenv/shims/rails s -e production'